# W2 C10 时间盒与引擎冻结记录（2026-06-13）

## 结论

- T1 插桩完成：CSR probe 阶段计时进入 `metrics.snapshot_json()`，覆盖 setup / bloom / offset / body / total，以及 bloom-negative / offset-miss / body-hit 三类结局计数。
- SF30 schema/budg-b64 双店导入完成并保留：schema 19:04.36、MaxRSS 2.31GB；budg-b64 19:11.32、MaxRSS 2.50GB；final flush 1.6/1.7s。
- D1 结论：当前固定开销仍显著高于 50us/probe，主要在 offset lookup；尝试的 4KB offset-window fix 未达保留线且增加 read_bytes，已回滚。
- W6 runner 已就绪：`baseline/run_w6_sf100_matrix_20260613.sh`；SF1 dry-run 9 变体全绿，3 repeats，A/B/A sentinel，8 个 compare 全部 mismatch=0。

## 代码改动

- `src/metrics.rs`
  - 新增 `csr_probe_setup_latency`、`csr_probe_bloom_latency`、`csr_probe_offset_lookup_latency`、`csr_probe_body_read_latency`、`csr_probe_total_latency`。
  - 新增 `csr_probe_bloom_negative`、`csr_probe_offset_miss`、`csr_probe_body_hit`。
  - JSON snapshot 和 reset 全覆盖。
- `src/csr/reader.rs`
  - 在 `get_neighbors*` / `lookup_offset` 路径记录阶段耗时和结局。
  - 单测断言 body-hit、Bloom-negative、Bloom false-positive offset-miss、warm cache zero offset bytes。
- `src/bin/lsmgraph.rs`
  - `storage-bench` 新增可选 `--l0-layout`，默认行为不变。
  - `--l0-layout oracle` 时 bench 前构建 oracle index，并在 JSON 写出 `oracle_index_stats`。
- `baseline/run_w6_sf100_matrix_20260613.sh`
  - 正式 W6 runner；SF100 默认锁住，需 `W6_ALLOW_SF100=1` 才能启动。

## 验证

- `nice -n 10 cargo test -j 16`：通过。
- `nice -n 10 cargo build --release -j 16 --bin lsmgraph`：通过，仅保留既有 `base_graph/csr.rs` 两个 warning。
- perf：`/proc/sys/kernel/perf_event_paranoid=3`，按工单不使用 sudo，采用插桩计数器。

## SF1 smoke

输出：`remote-logs/w2-c10-20260613/sf1-smoke-summary.tsv`

| variant | probe_total_avg_us | setup | bloom | offset | body | read_bytes | candidate_l0 |
|---|---:|---:|---:|---:|---:|---:|---:|
| schema | 440.03 | 2.44 | 3.86 | 394.57 | 36.37 | 43,000,792 | 320,515 |
| budg-b64 | 466.20 | 8.08 | 10.68 | 408.22 | 36.51 | 17,347,808 | 53,006 |

## SF30 profile

输出：

- `remote-logs/w2-c10-20260613/sf30-stage-summary.tsv`
- `remote-logs/w2-c10-20260613/sf30-schema/`
- `remote-logs/w2-c10-20260613/sf30-budg-b64/`

| variant | probe_total_avg_us | setup | bloom | offset_when_present | body_when_present | offset_count | body_hit | bloom_negative | offset_miss | read_bytes | candidate_l0 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| schema | 671.06 | 1.66 | 48.93 | 597.79 | 38.41 | 12,634 | 12,439 | 368 | 195 | 463,973,368 | 75,649 |
| budg-b64 | 690.35 | 2.05 | 56.05 | 611.70 | 39.91 | 11,542 | 11,332 | 396 | 210 | 461,586,920 | 77,919 |

Interpretation:

- Fixed overhead is not below the D1 50us/probe skip line.
- Offset lookup dominates. Bloom is no longer the SF1-era repeated SourceBloom reload cliff; cache hits are high and misses are bounded.
- This should be recorded as a mechanism observation for future read-path optimization, not as a blocker for W6.

## Optimization Attempt

Tried one fix: estimated 4KB offset window before binary-search fallback.

Output: `remote-logs/w2-c10-20260613/sf30-stage-summary-opt-window.tsv`

| variant | before_total | after_total | result | before_read_bytes | after_read_bytes |
|---|---:|---:|---|---:|---:|
| schema | 671.06 | 714.38 | worse, reverted | 463,973,368 | 515,165,656 |
| budg-b64 | 690.35 | 696.37 | <15%, reverted | 461,586,920 | 507,763,160 |

Decision: fix failed the time-box rule and was reverted.

## W6 Dry-Run

Runner: `baseline/run_w6_sf100_matrix_20260613.sh`

Dry-run command:

```bash
SCALE=sf1 DRY_RUN=1 RUN_ID=w6-sf1-dryrun-20260613 SAMPLES=20 REPEATS=3 RUN_COMPARE=1 KEEP_STORES=0 \
  nice -n 10 baseline/run_w6_sf100_matrix_20260613.sh
```

Output: `remote-logs/w6-sf1-dryrun-20260613/`

- Variants: schema, naive, kv-lsm, edge-type-only, semantic, budg-b64, budg-b256, budg-b1024, oracle.
- A/B/A sentinels: `sentinel-A1.json`, `sentinel-B.json`, `sentinel-A2.json`.
- Compare: 8 non-schema variants, each `checked=180`, `mismatches=0`.
- Oracle bench includes `oracle_index_stats.entry_count=11330230`.
- DONE timestamp: 2026-06-13T03:00:13+08:00.

## W6 放行清单

- Engine freeze commit/tag required after this report lands.
- W6 runner dry-run is green.
- SF100 is still locked by script unless `W6_ALLOW_SF100=1`.
- Expected W6 launch command:

```bash
W6_ALLOW_SF100=1 SCALE=sf100 RUN_ID=w6-sf100-matrix-20260613 SAMPLES=5000 REPEATS=3 RUN_COMPARE=0 \
  nice -n 10 baseline/run_w6_sf100_matrix_20260613.sh
```

Recommended W6 worker rule: one SF100 task only, poll `progress.log`, abort if `/data` free <200GiB, MemAvailable <80GiB, or any import exceeds its timeout/progress stalls.
