① 你最初担心的事（LDBC benchmark 查询走 adjacency cache 而非 CSR）—— 确实已彻底消除。
所有运行时入口都走 open_dynamic：
- snb-server（lsmgraph.rs:419）
- snb-validate / snb-validate-batch / snb-validate-mixed（lsmgraph.rs:364/381/397）全部分发到 *_dynamic 版本

这条链 100% 是 DynamicSnbGraph → BaseGraph CSR (+Delta)，已无 adjacency cache、也无 fallback_engine（上次 commit 已删）。

② 但 "Legacy 这部分功能完全不使用" —— 代码层面并不成立，还有三处活体残留：

┌─────────────────────────────────────────────┬────────────────────────┬─────────────────────────────────────────────┐
│                    残留                     │          位置          │                  是否可达                   │
├─────────────────────────────────────────────┼────────────────────────┼─────────────────────────────────────────────┤
│ SnbGraph::Legacy variant + LegacySnbGraph   │ props.rs:218/2522 起   │ 编译进二进制                                │
│ 全套 match 臂                               │                        │                                             │
├─────────────────────────────────────────────┼────────────────────────┼─────────────────────────────────────────────┤
│ 三个 legacy 验证函数 validate_ic1_ic14 /    │                        │ 死代码：mod.rs 仍 re-export，但没有任何     │
│ validate_ic_batch /                         │ queries.rs:300/324/435 │ CLI/server/test 调用，只有 _dynamic         │
│ validate_mixed_tugraph（调 SnbGraph::open） │                        │ 版被接上                                    │
├─────────────────────────────────────────────┼────────────────────────┼─────────────────────────────────────────────┤
│ snb-cache 命令 → build_adjacency_cache →    │                        │ 活 CLI 命令：没脚本调用它，但用户手敲       │
│ LegacySnbGraph                              │ lsmgraph.rs:431        │ lsmgraph snb-cache 仍会跑老的               │
│                                             │                        │ adjacency-cache 构建器                      │
└─────────────────────────────────────────────┴────────────────────────┴─────────────────────────────────────────────┘

所以准确回答你

- LDBC 验证/benchmark/server 的实际运行路径：已完全不碰 Legacy，可以放心。
- SnbGraph::Legacy / LegacySnbGraph / snb-cache 这套代码：还在仓库里编译、snb-cache 命令还能被手动触发。它不在你跑的那条链上，但说"完全不使用/可以当不存在"是不准确的——它属于未清理的死/半死代码。

要不要我把它清掉？范围可以分两档：
1. 保守：只删 3 个无人调用的 legacy 验证函数 + mod.rs re-export（纯死代码，零风险）。
2. 彻底：连 snb-cache 命令、SnbGraph::Legacy variant、LegacySnbGraph 整个结构一起删——改动大、要确认你以后不再需要这个独立的 adjacency-cache dump 工具。